# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# Data — never commit the raw dataset
data/raw/*
!data/raw/.gitkeep
*.duckdb
*.duckdb.wal

# Node
node_modules/
web/dist/
web/playwright-report/
web/test-results/
web/.vite/

# Env
.env
.env.local

# OS / editor
.DS_Store
.idea/

# Playwright
web/test-results/
web/playwright-report/
web/.playwright/

# Audit store (materialised from the log; rebuildable)
data/audit/

# Incremental build state, regenerated by tsc on every build.
*.tsbuildinfo

# The end-to-end suite materialises a real audit log into the fixture tree.
tests/fixtures/data/audit/
